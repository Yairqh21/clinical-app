import { CustomPercentagePipe } from './custom-percentage.pipe';

describe('CustomPercentagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
